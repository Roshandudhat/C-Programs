//write a program to used con() in math.
#include<stdio.h>
#include<math.h>

int main() 
{
    printf("The cosine of %lf is %lf \n", 2.3, cos(2.3));
    printf("The cosine of %lf is %lf \n", -2.3, cos(-2.3));
    double result=cos(90.0 * (3.14159265 / 180.0));
    printf("The cosine of %lf is %lf \n", 90.0, result);
    s
}
